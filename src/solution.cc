#include "solution.hpp"

std::vector<Log> ReadLogs(const std::string& filename) { return {}; }

std::vector<Log> Filter(const std::vector<Log>& logs, int level) { return {}; }

std::vector<Log> Filter(const std::vector<Log>& logs,
                        DateTime start,
                        DateTime end) {
  return {};
}

void WriteMetrics(const std::vector<Log>& logs, const std::string& filename) {}
